<%= form_with model: [@commentable, @comment] do |f| %>
  <%= f.text_area :content %>
  <div class="actions">
    <%= f.submit "#{t('views.common.title_edit', name: Comment.model_name.human)}", class: 'btn btn-warning px-5' %>
  </div>
<% end %>
