<%= form_with model: [@commentable, @comment] do |f| %>
  <%= f.text_area :content %>
  <div class="actions">
    <%= f.submit "#{Comment.model_name.human}#{I18n.t("helpers.submit.update")}", class: 'btn btn-warning px-5' %>
  </div>
<% end %>
