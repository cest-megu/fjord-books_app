<%= form_with model: [commentable, commentable.comments.new] do |f| %>
  <%= f.text_area :content %>
  <div class="actions">
    <%= f.submit "#{t('views.common.title_new', name: Comment.model_name.human)}", class: 'btn btn-warning px-5' %>
  </div>
<% end %>
